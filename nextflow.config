profiles {
  slurm {
    process {
      executor = 'slurm'
      queue = 'work'
      clusterOptions = '--account=pawsey1227'
    }
  }
  
  pbs {
    process {
      executor = 'pbs'
      queue = 'workq'
      clusterOptions = '-P ad78'
    }
  }
}